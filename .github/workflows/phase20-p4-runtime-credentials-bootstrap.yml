name: Phase 20 P4 runtime credentials bootstrap

on:
  push:
    branches:
      - feat/phase20-p4-runtime-plugin-credentials

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Implement runtime plugin credentials
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected exactly one match, found {count}")
              return text.replace(old, new, 1)

          # Core: allow explicit per-child environment overrides without mutating the parent process.
          path = Path("crates/omnicreator-core/src/plugin_process.rs")
          text = path.read_text()
          old = '''    pub fn spawn(plugin: &DiscoveredPlugin, options: PluginProcessOptions) -> Result<Self> {
        plugin.manifest.validate_v1()?;
        options.validate()?;

        let mut command = Command::new(&plugin.manifest.entrypoint.command);
        command
            .args(&plugin.manifest.entrypoint.args)
            .current_dir(&plugin.directory)
            .stdin(Stdio::piped())
            .stdout(Stdio::piped())
            .stderr(Stdio::piped());
'''
          new = '''    pub fn spawn(plugin: &DiscoveredPlugin, options: PluginProcessOptions) -> Result<Self> {
        Self::spawn_with_env(plugin, options, &BTreeMap::new())
    }

    pub fn spawn_with_env(
        plugin: &DiscoveredPlugin,
        options: PluginProcessOptions,
        environment: &BTreeMap<String, String>,
    ) -> Result<Self> {
        plugin.manifest.validate_v1()?;
        options.validate()?;

        let mut command = Command::new(&plugin.manifest.entrypoint.command);
        command
            .args(&plugin.manifest.entrypoint.args)
            .envs(environment)
            .current_dir(&plugin.directory)
            .stdin(Stdio::piped())
            .stdout(Stdio::piped())
            .stderr(Stdio::piped());
'''
          text = replace_once(text, old, new, "plugin spawn env override")
          text = replace_once(text, "import json\nimport sys\nimport time\n", "import json\nimport os\nimport sys\nimport time\n", "fixture os import")
          old = '''    result = {"method": method, "params": request.get("params")}
    print(json.dumps({
'''
          new = '''    result = {"method": method, "params": request.get("params")}
    if method == "fixture.env":
        result["runtime_key"] = os.environ.get("OMNICREATOR_TEST_RUNTIME_KEY")
    print(json.dumps({
'''
          text = replace_once(text, old, new, "fixture env result")
          marker = '''    #[test]
    fn timeout_does_not_break_later_request_correlation() {
'''
          test = '''    #[test]
    fn explicit_runtime_environment_is_scoped_to_plugin_child() {
        let Some((_temp, plugin)) = fixture_plugin() else {
            return;
        };
        let environment = BTreeMap::from([(
            "OMNICREATOR_TEST_RUNTIME_KEY".to_owned(),
            "runtime-secret".to_owned(),
        )]);
        let process = PluginProcess::spawn_with_env(&plugin, process_options(), &environment).unwrap();

        let result = process.call("fixture.env", Value::Null).unwrap();
        match result.response {
            PluginResponse::Success { result, .. } => {
                assert_eq!(result["runtime_key"], "runtime-secret");
            }
            PluginResponse::Failure { .. } => panic!("fixture returned failure"),
        }

        process.shutdown().unwrap();
    }

'''
          text = replace_once(text, marker, test + marker, "plugin env regression test")
          path.write_text(text)

          # Desktop backend: memory-only credential overlay + safe sanitized projection.
          path = Path("apps/desktop/src-tauri/src/application.rs")
          text = path.read_text()
          text = replace_once(
              text,
              '''struct DesktopState {
    active: Mutex<Option<ActiveWorkspace>>,
    compute: Mutex<Option<ComputeProviderRuntime<HttpComputeProvider>>>,
}''',
              '''struct DesktopState {
    active: Mutex<Option<ActiveWorkspace>>,
    compute: Mutex<Option<ComputeProviderRuntime<HttpComputeProvider>>>,
    runtime_plugin_credentials: Mutex<BTreeMap<String, String>>,
}''',
              "desktop runtime credential state",
          )
          text = replace_once(
              text,
              '''struct PluginRuntimeReadinessDesktopViewV1 {
    plugin_id: String,
    status: String,
    reason_code: Option<String>,
}''',
              '''struct PluginRuntimeReadinessDesktopViewV1 {
    plugin_id: String,
    status: String,
    reason_code: Option<String>,
    credentials: Vec<PluginRuntimeCredentialDesktopViewV1>,
}

#[derive(Debug, Serialize)]
struct PluginRuntimeCredentialDesktopViewV1 {
    env_name: String,
    source: String,
}''',
              "desktop runtime credential view",
          )

          old = '''#[tauri::command]
fn plugin_inventory(app: AppHandle) -> Result<PluginInventoryDesktopViewV1, String> {
    let report = plugin_inventory_report_v1(&app)?;
    let runtime = studio_pack_runtime_snapshot_v1(&app, &report.registry)?;
    let readiness = report
        .inventory
        .iter()
        .map(|plugin| {
            let (status, reason_code) = match runtime.get_v1(&plugin.id) {
                PluginRuntimeReadinessV1::Ready => ("ready".to_owned(), None),
                PluginRuntimeReadinessV1::SetupRequired { reason_code } => {
                    ("setup_required".to_owned(), Some(reason_code))
                }
                PluginRuntimeReadinessV1::Unavailable { reason_code } => {
                    ("unavailable".to_owned(), Some(reason_code))
                }
            };
            PluginRuntimeReadinessDesktopViewV1 {
                plugin_id: plugin.id.clone(),
                status,
                reason_code,
            }
        })
        .collect();

    Ok(PluginInventoryDesktopViewV1 {
        plugins: report.inventory,
        diagnostics: report
            .diagnostics
            .into_iter()
            .map(|diagnostic| PluginDiagnosticDesktopViewV1 {
                code: diagnostic.code.as_str().to_owned(),
                path: diagnostic.path.to_string_lossy().into_owned(),
                message: diagnostic.message,
            })
            .collect(),
        readiness,
    })
}
'''
          new = '''fn plugin_credential_env_names_v1(plugin: &DiscoveredPlugin) -> Vec<String> {
    let report = load_plugin_settings_ui(plugin);
    if !report.diagnostics.is_empty() {
        return Vec::new();
    }
    let mut names = report
        .ui
        .as_ref()
        .map(|ui| {
            ui.fields
                .iter()
                .filter(|field| field.key.ends_with("_env"))
                .filter_map(|field| field.default.as_ref()?.as_str())
                .map(str::trim)
                .filter(|name| !name.is_empty())
                .map(str::to_owned)
                .collect::<Vec<_>>()
        })
        .unwrap_or_default();
    names.sort();
    names.dedup();
    names
}

fn runtime_plugin_credentials_snapshot_v1(
    state: &State<'_, DesktopState>,
) -> Result<BTreeMap<String, String>, String> {
    state
        .runtime_plugin_credentials
        .lock()
        .map_err(lock_error)
        .map(|guard| guard.clone())
}

fn runtime_credential_source_v1(
    runtime_credentials: &BTreeMap<String, String>,
    env_name: &str,
) -> &'static str {
    if runtime_credentials
        .get(env_name)
        .is_some_and(|value| !value.trim().is_empty())
    {
        "runtime"
    } else if env::var(env_name).is_ok_and(|value| !value.trim().is_empty()) {
        "environment"
    } else {
        "missing"
    }
}

fn plugin_runtime_credential_views_v1(
    plugin: &DiscoveredPlugin,
    runtime_credentials: &BTreeMap<String, String>,
) -> Vec<PluginRuntimeCredentialDesktopViewV1> {
    plugin_credential_env_names_v1(plugin)
        .into_iter()
        .map(|env_name| PluginRuntimeCredentialDesktopViewV1 {
            source: runtime_credential_source_v1(runtime_credentials, &env_name).to_owned(),
            env_name,
        })
        .collect()
}

fn validate_plugin_runtime_credential_target_v1(
    report: &PluginInventoryReportV1,
    plugin_id: &str,
    credential_env: &str,
) -> Result<(), String> {
    let plugin = report
        .registry
        .get(plugin_id)
        .ok_or_else(|| format!("Plugin {plugin_id} is not installed on this machine."))?;
    if !plugin_credential_env_names_v1(plugin)
        .iter()
        .any(|name| name == credential_env)
    {
        return Err(format!(
            "Plugin {plugin_id} does not declare runtime credential {credential_env}."
        ));
    }
    Ok(())
}

fn plugin_inventory_view_v1(
    app: &AppHandle,
    state: &State<'_, DesktopState>,
) -> Result<PluginInventoryDesktopViewV1, String> {
    let report = plugin_inventory_report_v1(app)?;
    let runtime = studio_pack_runtime_snapshot_v1(app, state, &report.registry)?;
    let runtime_credentials = runtime_plugin_credentials_snapshot_v1(state)?;
    let readiness = report
        .inventory
        .iter()
        .map(|plugin| {
            let (status, reason_code) = match runtime.get_v1(&plugin.id) {
                PluginRuntimeReadinessV1::Ready => ("ready".to_owned(), None),
                PluginRuntimeReadinessV1::SetupRequired { reason_code } => {
                    ("setup_required".to_owned(), Some(reason_code))
                }
                PluginRuntimeReadinessV1::Unavailable { reason_code } => {
                    ("unavailable".to_owned(), Some(reason_code))
                }
            };
            let credentials = report
                .registry
                .get(&plugin.id)
                .map(|discovered| {
                    plugin_runtime_credential_views_v1(discovered, &runtime_credentials)
                })
                .unwrap_or_default();
            PluginRuntimeReadinessDesktopViewV1 {
                plugin_id: plugin.id.clone(),
                status,
                reason_code,
                credentials,
            }
        })
        .collect();

    Ok(PluginInventoryDesktopViewV1 {
        plugins: report.inventory,
        diagnostics: report
            .diagnostics
            .into_iter()
            .map(|diagnostic| PluginDiagnosticDesktopViewV1 {
                code: diagnostic.code.as_str().to_owned(),
                path: diagnostic.path.to_string_lossy().into_owned(),
                message: diagnostic.message,
            })
            .collect(),
        readiness,
    })
}

#[tauri::command]
fn plugin_inventory(
    app: AppHandle,
    state: State<'_, DesktopState>,
) -> Result<PluginInventoryDesktopViewV1, String> {
    plugin_inventory_view_v1(&app, &state)
}

#[tauri::command]
fn set_plugin_runtime_credential(
    app: AppHandle,
    state: State<'_, DesktopState>,
    plugin_id: String,
    credential_env: String,
    value: String,
) -> Result<PluginInventoryDesktopViewV1, String> {
    let plugin_id = plugin_id.trim();
    let credential_env = credential_env.trim();
    if plugin_id.is_empty() || credential_env.is_empty() {
        return Err("Plugin id and credential environment name must not be empty.".to_owned());
    }
    if value.trim().is_empty() {
        return Err("Runtime API key must not be empty.".to_owned());
    }
    let report = plugin_inventory_report_v1(&app)?;
    validate_plugin_runtime_credential_target_v1(&report, plugin_id, credential_env)?;
    state
        .runtime_plugin_credentials
        .lock()
        .map_err(lock_error)?
        .insert(credential_env.to_owned(), value);
    plugin_inventory_view_v1(&app, &state)
}

#[tauri::command]
fn clear_plugin_runtime_credential(
    app: AppHandle,
    state: State<'_, DesktopState>,
    plugin_id: String,
    credential_env: String,
) -> Result<PluginInventoryDesktopViewV1, String> {
    let plugin_id = plugin_id.trim();
    let credential_env = credential_env.trim();
    if plugin_id.is_empty() || credential_env.is_empty() {
        return Err("Plugin id and credential environment name must not be empty.".to_owned());
    }
    let report = plugin_inventory_report_v1(&app)?;
    validate_plugin_runtime_credential_target_v1(&report, plugin_id, credential_env)?;
    state
        .runtime_plugin_credentials
        .lock()
        .map_err(lock_error)?
        .remove(credential_env);
    plugin_inventory_view_v1(&app, &state)
}
'''
          text = replace_once(text, old, new, "plugin inventory runtime credential commands")

          # Lifecycle commands return a refreshed state-aware view.
          for signature_old, signature_new in [
              (
                  '''fn set_plugin_enabled(\n    app: AppHandle,\n    plugin_id: String,''',
                  '''fn set_plugin_enabled(\n    app: AppHandle,\n    state: State<'_, DesktopState>,\n    plugin_id: String,''',
              ),
              (
                  '''fn install_plugin_from_folder(\n    app: AppHandle,\n    source_path: String,''',
                  '''fn install_plugin_from_folder(\n    app: AppHandle,\n    state: State<'_, DesktopState>,\n    source_path: String,''',
              ),
              (
                  '''fn uninstall_plugin(\n    app: AppHandle,\n    plugin_id: String,''',
                  '''fn uninstall_plugin(\n    app: AppHandle,\n    state: State<'_, DesktopState>,\n    plugin_id: String,''',
              ),
              (
                  '''fn apply_plugin_update(\n    app: AppHandle,\n    plugin_id: String,''',
                  '''fn apply_plugin_update(\n    app: AppHandle,\n    state: State<'_, DesktopState>,\n    plugin_id: String,''',
              ),
          ]:
              text = replace_once(text, signature_old, signature_new, "state-aware plugin lifecycle command")
          if text.count("    plugin_inventory(app)\n") != 4:
              raise SystemExit(f"expected four lifecycle plugin_inventory refreshes, found {text.count('    plugin_inventory(app)')}")
          text = text.replace("    plugin_inventory(app)\n", "    plugin_inventory_view_v1(&app, &state)\n")

          old = '''fn studio_pack_runtime_snapshot_v1(
    app: &AppHandle,
    registry: &PluginRegistry,
) -> Result<StudioPackRuntimeSnapshotV1, String> {
    let lifecycle = load_plugin_lifecycle_v1(app)?;
    let mut runtime = StudioPackRuntimeSnapshotV1::default();
'''
          new = '''fn studio_pack_runtime_snapshot_v1(
    app: &AppHandle,
    state: &State<'_, DesktopState>,
    registry: &PluginRegistry,
) -> Result<StudioPackRuntimeSnapshotV1, String> {
    let lifecycle = load_plugin_lifecycle_v1(app)?;
    let runtime_credentials = runtime_plugin_credentials_snapshot_v1(state)?;
    let mut runtime = StudioPackRuntimeSnapshotV1::default();
'''
          text = replace_once(text, old, new, "runtime snapshot signature")
          old = '''                    let present = env::var(name)
                        .map(|value| !value.trim().is_empty())
                        .unwrap_or(false);
                    (!present).then(|| name.to_owned())
'''
          new = '''                    let present =
                        runtime_credential_source_v1(&runtime_credentials, name) != "missing";
                    (!present).then(|| name.to_owned())
'''
          text = replace_once(text, old, new, "runtime readiness credential overlay")

          # Every runtime snapshot is now state-aware. These call sites all already own State.
          replacements = {
              "studio_pack_runtime_snapshot_v1(&app, &registry)?": "studio_pack_runtime_snapshot_v1(&app, &state, &registry)?",
              "studio_pack_runtime_snapshot_v1(app, &registry)?": "studio_pack_runtime_snapshot_v1(app, state, &registry)?",
              "studio_pack_runtime_snapshot_v1(&app, &inventory.registry)?": "studio_pack_runtime_snapshot_v1(&app, &state, &inventory.registry)?",
              "studio_pack_runtime_snapshot_v1(app, &inventory.registry)?": "studio_pack_runtime_snapshot_v1(app, state, &inventory.registry)?",
          }
          for old_call, new_call in replacements.items():
              text = text.replace(old_call, new_call)

          # The creator visual runtime carries a snapshot of runtime-only keys and passes only
          # credentials declared by the selected plugin into that child process.
          text = replace_once(
              text,
              '''struct DesktopVisualRuntimeV1<'a> {
    registry: &'a PluginRegistry,
    runtime: &'a StudioPackRuntimeSnapshotV1,
    runtime_root: PathBuf,
}''',
              '''struct DesktopVisualRuntimeV1<'a> {
    registry: &'a PluginRegistry,
    runtime: &'a StudioPackRuntimeSnapshotV1,
    runtime_root: PathBuf,
    runtime_credentials: BTreeMap<String, String>,
}''',
              "visual runtime credential snapshot",
          )
          marker = '''    fn process_result_v1(
        plugin: &DiscoveredPlugin,
'''
          helper = '''    fn spawn_plugin_v1(&self, plugin: &DiscoveredPlugin) -> CoreResult<PluginProcess> {
        let declared = plugin_credential_env_names_v1(plugin)
            .into_iter()
            .collect::<BTreeSet<_>>();
        let environment = self
            .runtime_credentials
            .iter()
            .filter(|(name, value)| declared.contains(*name) && !value.trim().is_empty())
            .map(|(name, value)| (name.clone(), value.clone()))
            .collect::<BTreeMap<_, _>>();
        PluginProcess::spawn_with_env(plugin, PluginProcessOptions::default(), &environment)
    }

'''
          text = replace_once(text, marker, helper + marker, "visual runtime child env helper")
          count = text.count("PluginProcess::spawn(plugin, PluginProcessOptions::default())")
          if count != 3:
              raise SystemExit(f"expected three Desktop plugin spawn sites, found {count}")
          text = text.replace(
              "PluginProcess::spawn(plugin, PluginProcessOptions::default())",
              "self.spawn_plugin_v1(plugin)",
          )

          # creator_visual_plan_for_desktop_v1 now takes State, returns runtime credential snapshot.
          text = replace_once(
              text,
              '''fn creator_visual_plan_for_desktop_v1(
    app: &AppHandle,
    store: &StateStore,''',
              '''fn creator_visual_plan_for_desktop_v1(
    app: &AppHandle,
    state: &State<'_, DesktopState>,
    store: &StateStore,''',
              "creator visual plan state parameter",
          )
          text = replace_once(
              text,
              '''        StudioPackRuntimeSnapshotV1,
        PathBuf,
    ),''',
              '''        StudioPackRuntimeSnapshotV1,
        PathBuf,
        BTreeMap<String, String>,
    ),''',
              "creator visual plan return credentials",
          )
          text = replace_once(
              text,
              '''    let runtime_root = creator_plugin_runtime_root_v1(app)?;
    let visual_runtime = DesktopVisualRuntimeV1 {
        registry: &inventory.registry,
        runtime: &runtime,
        runtime_root: runtime_root.clone(),
    };''',
              '''    let runtime_root = creator_plugin_runtime_root_v1(app)?;
    let runtime_credentials = runtime_plugin_credentials_snapshot_v1(state)?;
    let visual_runtime = DesktopVisualRuntimeV1 {
        registry: &inventory.registry,
        runtime: &runtime,
        runtime_root: runtime_root.clone(),
        runtime_credentials: runtime_credentials.clone(),
    };''',
              "creator visual plan runtime credentials",
          )
          text = replace_once(
              text,
              '''    Ok((creator, plan, inventory, runtime, runtime_root))''',
              '''    Ok((
        creator,
        plan,
        inventory,
        runtime,
        runtime_root,
        runtime_credentials,
    ))''',
              "creator visual plan return tuple",
          )
          text = text.replace(
              "creator_visual_plan_for_desktop_v1(&app, &store, &artifacts, &project_id)?",
              "creator_visual_plan_for_desktop_v1(&app, &state, &store, &artifacts, &project_id)?",
          )
          text = text.replace(
              "creator_visual_plan_for_desktop_v1(&app, &store, &artifacts, &project_id)?;",
              "creator_visual_plan_for_desktop_v1(&app, &state, &store, &artifacts, &project_id)?;",
          )
          text = text.replace(
              "let (creator, plan, _, _, _) =",
              "let (creator, plan, _, _, _, _) =",
          )
          text = text.replace(
              "let (creator, mut plan, inventory, runtime, runtime_root) =",
              "let (creator, mut plan, inventory, runtime, runtime_root, runtime_credentials) =",
          )
          # Two execute paths reconstruct the runtime after the tuple return.
          old_runtime = '''    let visual_runtime = DesktopVisualRuntimeV1 {
        registry: &inventory.registry,
        runtime: &runtime,
        runtime_root,
    };'''
          new_runtime = '''    let visual_runtime = DesktopVisualRuntimeV1 {
        registry: &inventory.registry,
        runtime: &runtime,
        runtime_root,
        runtime_credentials,
    };'''
          if text.count(old_runtime) != 2:
              raise SystemExit(f"expected two visual runtime reconstruction sites, found {text.count(old_runtime)}")
          text = text.replace(old_runtime, new_runtime)

          # start_creator_production constructs a runtime directly.
          old = '''        let visual_runtime = DesktopVisualRuntimeV1 {
            registry: &inventory.registry,
            runtime: &plugin_runtime,
            runtime_root,
        };'''
          new = '''        let visual_runtime = DesktopVisualRuntimeV1 {
            registry: &inventory.registry,
            runtime: &plugin_runtime,
            runtime_root,
            runtime_credentials: runtime_plugin_credentials_snapshot_v1(&state)?,
        };'''
          text = replace_once(text, old, new, "production visual runtime credentials")

          # Add Tauri commands.
          text = replace_once(
              text,
              '''            plugin_inventory,
            set_plugin_enabled,''',
              '''            plugin_inventory,
            set_plugin_runtime_credential,
            clear_plugin_runtime_credential,
            set_plugin_enabled,''',
              "runtime credential invoke handlers",
          )

          # Guard against missed old runtime snapshot calls or unsafe secret persistence.
          if "studio_pack_runtime_snapshot_v1(&app, &registry)?" in text:
              raise SystemExit("missed state-less runtime snapshot call")
          path.write_text(text)

          # Frontend: masked session-only Set/Clear controls on each declared credential.
          path = Path("apps/desktop/dist/app.js")
          text = path.read_text()
          marker = '''function pluginCardMarkup(view, plugin) {
'''
          helper = '''function pluginRuntimeCredentialMarkup(readiness, plugin) {
  const credentials = Array.isArray(readiness && readiness.credentials)
    ? readiness.credentials
    : [];
  if (!credentials.length) return "";
  return (
    '<div class="plugin-runtime-credentials"><span class="plugin-runtime-credentials-title">RUNTIME API KEYS</span>' +
    credentials
      .map(function (credential) {
        const source = credential.source || "missing";
        const sourceLabel =
          source === "runtime"
            ? "Runtime key active"
            : source === "environment"
              ? "OS environment active"
              : "Key required";
        const placeholder =
          source === "runtime"
            ? "Paste a replacement key for this session"
            : source === "environment"
              ? "Paste a temporary override for this session"
              : "Paste API key for this OmniCreator session";
        return (
          '<div class="plugin-runtime-credential" data-plugin-id="' +
          escapeHtml(plugin.id) +
          '" data-credential-env="' +
          escapeHtml(credential.env_name) +
          '"><div class="plugin-runtime-credential-head"><code>' +
          escapeHtml(credential.env_name) +
          '</code><span class="plugin-credential-source ' +
          escapeHtml(source) +
          '">' +
          escapeHtml(sourceLabel) +
          '</span></div><div class="plugin-runtime-credential-form"><input class="plugin-runtime-key-input" type="password" autocomplete="off" spellcheck="false" placeholder="' +
          escapeHtml(placeholder) +
          '" aria-label="Runtime API key for ' +
          escapeHtml(credential.env_name) +
          '" /><button class="btn primary plugin-runtime-key-set" type="button">Set runtime key</button>' +
          (source === "runtime"
            ? '<button class="btn plugin-runtime-key-clear" type="button">Clear runtime key</button>'
            : "") +
          '</div><small>Memory only. Cleared when OmniCreator exits. The key is never written to the Data Root.</small></div>'
        );
      })
      .join("") +
    "</div>"
  );
}

'''
          text = replace_once(text, marker, helper + marker, "runtime credential UI helper")
          old = '''    (readiness.reason_code
      ? '<div class="notice subtle plugin-setup-reason">Setup / readiness: ' +
        escapeHtml(readiness.reason_code) +
        "</div>"
      : "") +
    '<div class="plugin-contract-section"><span>Types</span>' +
'''
          new = '''    (readiness.reason_code
      ? '<div class="notice subtle plugin-setup-reason">Setup / readiness: ' +
        escapeHtml(readiness.reason_code) +
        "</div>"
      : "") +
    pluginRuntimeCredentialMarkup(readiness, plugin) +
    '<div class="plugin-contract-section"><span>Types</span>' +
'''
          text = replace_once(text, old, new, "runtime credential card placement")
          marker = '''  document.querySelectorAll(".plugin-disable").forEach(function (button) {
'''
          handlers = '''  document.querySelectorAll(".plugin-runtime-key-set").forEach(function (button) {
    button.onclick = async function () {
      const row = button.closest(".plugin-runtime-credential");
      const input = row && row.querySelector(".plugin-runtime-key-input");
      const value = input ? input.value : "";
      if (!value.trim()) {
        showToast("Paste an API key before setting the runtime credential.");
        if (input) input.focus();
        return;
      }
      button.disabled = true;
      try {
        const next = await call("set_plugin_runtime_credential", {
          pluginId: row.dataset.pluginId,
          credentialEnv: row.dataset.credentialEnv,
          value: value,
        });
        const readiness = pluginReadiness(next, row.dataset.pluginId);
        pluginManagerState.tab = readiness.status === "ready" ? "enabled" : "needs_attention";
        await refreshPluginManagerAfterMutation(next);
        showToast("Runtime API key applied for this OmniCreator session.");
      } finally {
        if (input) input.value = "";
      }
    };
  });

  document.querySelectorAll(".plugin-runtime-key-clear").forEach(function (button) {
    button.onclick = async function () {
      const row = button.closest(".plugin-runtime-credential");
      button.disabled = true;
      const next = await call("clear_plugin_runtime_credential", {
        pluginId: row.dataset.pluginId,
        credentialEnv: row.dataset.credentialEnv,
      });
      const plugin = (next.plugins || []).find(function (item) {
        return item.id === row.dataset.pluginId;
      });
      pluginManagerState.tab = plugin ? pluginManagerBucket(next, plugin) : "needs_attention";
      await refreshPluginManagerAfterMutation(next);
      showToast("Runtime API key cleared. Environment fallback was re-evaluated.");
    };
  });

'''
          text = replace_once(text, marker, handlers + marker, "runtime credential frontend handlers")
          path.write_text(text)

          # Styles.
          path = Path("apps/desktop/dist/styles.css")
          text = path.read_text()
          marker = '''.plugin-impact-preview {
  margin-bottom: 14px;
'''
          styles = '''.plugin-runtime-credentials {
  display: grid;
  gap: 9px;
  margin-top: 12px;
  padding: 12px;
  border: 1px solid #303743;
  border-radius: 12px;
  background: rgba(9, 13, 20, 0.28);
}
.plugin-runtime-credentials-title {
  color: #7f8997;
  font-size: 9px;
  font-weight: 850;
  letter-spacing: 0.08em;
}
.plugin-runtime-credential {
  display: grid;
  gap: 8px;
}
.plugin-runtime-credential + .plugin-runtime-credential {
  padding-top: 10px;
  border-top: 1px solid #2b313c;
}
.plugin-runtime-credential-head,
.plugin-runtime-credential-form {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}
.plugin-runtime-credential-head {
  justify-content: space-between;
}
.plugin-runtime-credential-head code {
  color: #d5dbe5;
  font-size: 10px;
}
.plugin-credential-source {
  font-size: 10px;
  font-weight: 750;
  color: #9da6b5;
}
.plugin-credential-source.runtime { color: #86efac; }
.plugin-credential-source.environment { color: #93c5fd; }
.plugin-credential-source.missing { color: #fde68a; }
.plugin-runtime-credential-form input {
  flex: 1 1 220px;
  min-width: 0;
  box-sizing: border-box;
  border: 1px solid #343c49;
  border-radius: 10px;
  padding: 9px 10px;
  background: #0d1117;
  color: #f4f5f7;
}
.plugin-runtime-credential-form .btn {
  flex: 0 0 auto;
}
.plugin-runtime-credential small {
  color: #7f8997;
  line-height: 1.45;
}

'''
          text = replace_once(text, marker, styles + marker, "runtime credential styles")
          path.write_text(text)

          # Static Desktop acceptance smoke.
          Path("apps/desktop/tests/plugin-runtime-credentials-smoke.mjs").write_text('''import assert from "node:assert/strict";
import fs from "node:fs";

const app = fs.readFileSync(new URL("../dist/app.js", import.meta.url), "utf8");
const rust = fs.readFileSync(new URL("../src-tauri/src/application.rs", import.meta.url), "utf8");
const core = fs.readFileSync(new URL("../../../crates/omnicreator-core/src/plugin_process.rs", import.meta.url), "utf8");

assert.match(app, /type=\\"password\\"/);
assert.match(app, /set_plugin_runtime_credential/);
assert.match(app, /clear_plugin_runtime_credential/);
assert.match(app, /Memory only\\. Cleared when OmniCreator exits/);
assert.doesNotMatch(app, /localStorage[^\\n]*runtime_plugin/i);
assert.match(rust, /runtime_plugin_credentials: Mutex<BTreeMap<String, String>>/);
assert.match(rust, /validate_plugin_runtime_credential_target_v1/);
assert.match(rust, /runtime_credential_source_v1/);
assert.match(rust, /spawn_with_env\\(plugin, PluginProcessOptions::default\\(\\), &environment\\)/);
assert.match(core, /pub fn spawn_with_env/);
assert.match(core, /\\.envs\\(environment\\)/);

console.log("runtime plugin credential smoke: ok");
''')

          # CI gate.
          path = Path(".github/workflows/ci.yml")
          text = path.read_text()
          marker = '''      - name: Desktop Phase 20 unified Settings regression
        run: node apps/desktop/tests/unified-settings-p20-smoke.mjs
'''
          addition = marker + '''      - name: Desktop runtime plugin credential regression
        run: node apps/desktop/tests/plugin-runtime-credentials-smoke.mjs
'''
          text = replace_once(text, marker, addition, "runtime credential CI gate")
          path.write_text(text)
          PY

      - name: Format Rust
        run: cargo fmt --all

      - name: JavaScript syntax and runtime credential smoke
        run: |
          node --check apps/desktop/dist/app.js
          node apps/desktop/tests/plugin-runtime-credentials-smoke.mjs

      - name: Core targeted tests
        run: cargo test -p omnicreator-core plugin_process

      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/omnicreator-core/src/plugin_process.rs \
                  apps/desktop/src-tauri/src/application.rs \
                  apps/desktop/dist/app.js \
                  apps/desktop/dist/styles.css \
                  apps/desktop/tests/plugin-runtime-credentials-smoke.mjs \
                  .github/workflows/ci.yml
          git commit -m "feat: add runtime plugin API keys"
          git push
